source 'http://rubygems.org'

gem 'rails', '3.0.5'

group :development do
  gem 'sqlite3'
  gem 'nifty-generators' # used for nifty layout
  gem 'jquery-rails', '>= 0.2.6' # generates our jQuery UJS file via `rails g jquery:install`
end
